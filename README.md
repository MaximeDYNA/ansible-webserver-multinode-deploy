# ansible-webserver-multinode-deploy
This project uses Vagrant and Ansible to automate the setup of a simple web infrastructure composed of three servers:  One Ansible controller server.  One host server where Apache HTTP Server is installed and configured.  One host server where a PostgreSQL database is installed and configured.
